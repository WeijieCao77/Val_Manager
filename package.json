{
  "name": "val-manager",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "无畏契约（VALORANT）电竞经理 — a browser-based VCT esports manager game",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "extract": "python3 scripts/extract.py",
    "audit": "python3 scripts/audit_data.py && npx tsx scripts/audit_feedback.ts 3 && npx tsx scripts/smoke.ts && npx tsx scripts/check_stats_sql.ts && npx tsx scripts/check_cards_api.ts && npx tsx scripts/check_offline_save.ts && npx tsx scripts/card_check.ts",
    "start": "node server.js",
    "dossier": "python3 scripts/fetch_vlr_profiles.py && python3 scripts/fetch_vlr_staff.py && python3 scripts/fetch_liquipedia_faces.py && python3 scripts/fetch_legend_faces.py && python3 scripts/fetch_faces.py && python3 scripts/build_dossier.py"
  },
  "dependencies": {
    "postgres": "^3.4.9",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.7",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "tsx": "^4.23.12",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
